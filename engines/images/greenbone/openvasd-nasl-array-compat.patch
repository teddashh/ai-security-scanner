diff --git a/rust/src/nasl/interpreter/nasl_value.rs b/rust/src/nasl/interpreter/nasl_value.rs
index 1ac0c26..7f9cc1f 100644
--- a/rust/src/nasl/interpreter/nasl_value.rs
+++ b/rust/src/nasl/interpreter/nasl_value.rs
@@ -221,13 +221,12 @@ impl NaslValue {
             // array indices as keys, so the non-number key does not
             // exist).
             if let Ok(index) = index.as_number() {
-                let index = index
+                let index: usize = index
                     .try_into()
                     .map_err(|_| ErrorKind::NegativeIndex(index))
                     .map_err(ExprError::rhs)?;
-                arr.get(index)
-                    .ok_or_else(|| ExprError::lhs(ErrorKind::ArrayOutOfRange(index)))
-                    .cloned()
+                // Legacy NASL returns NULL for an unset capture/array slot.
+                Ok(arr.get(index).cloned().unwrap_or(NaslValue::Null))
             } else {
                 Ok(NaslValue::Null)
             }
diff --git a/rust/src/nasl/interpreter/tests/mod.rs b/rust/src/nasl/interpreter/tests/mod.rs
index 88f4afb..14ad2c6 100644
--- a/rust/src/nasl/interpreter/tests/mod.rs
+++ b/rust/src/nasl/interpreter/tests/mod.rs
@@ -293,5 +293,10 @@ interpreter_test_err!(undefined_fn, "foo();");
 interpreter_test_err!(expected_str, "a =~ 5;");
 interpreter_test_err!(expected_array, "a = 5; a[3];");
 
-interpreter_test_err!(array_out_of_range, "a = [1, 2, 3]; a[3];");
+interpreter_test_ok!(
+    array_out_of_range,
+    "a = [1, 2, 3]; a[3];",
+    NaslValue::Array(vec![1.into(), 2.into(), 3.into()]),
+    NaslValue::Null,
+);
 interpreter_test_err!(negative_array_index, "a = [1, 2, 3]; a[-3];");
