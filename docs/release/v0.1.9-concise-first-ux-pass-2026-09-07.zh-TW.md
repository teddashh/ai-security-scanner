# Concise-first UI／UX 歷史紀錄（2026-09-07）

本檔只保存當時的產品變更、實際走查與未執行項目。版本與發布尺度由產品 owner 決定。

## 產品變更

- 修復 browser project reload 後遺失 `knownAssets`、導致 Coverage 顯示 0 items 的資料斷點。
- 新網站專案重新載入後會保留 exact target、scheme、port 與 path；候選資產仍保持未授權、未掃描狀態。
- 單一 guided website 的 Review 頁把 exact target、限制、Advanced 與 Start 放在首屏；已完成步驟預設收合，仍可用 `Edit inputs` 取回。
- New scan 只呈現必要輸入，optional details 收合；preview 明示不接觸 target，也不顯示假的結果數字或無效 Refresh。
- Public-record、network、local 與 cloud 路徑仍保留各自必要的範圍與限制資訊。

產品程式碼 checkpoint：`b20cbfab5fdbe770b52cebcbfea1efae338f5325`。

## 實際走查

在 browser dev surface 建立 `https://example.com` 網站專案，重新載入後確認 Coverage 保留 1 個 exact item；接著檢查 Review、Advanced、Edit inputs、Back to review 與 Start preview。Preview 拒絕真正執行，且沒有接觸 target。

## 驗證結果

- Frontend `493/493`；component `156/156`。
- TypeScript、production frontend build 與 Windows Tauri debug build 通過。
- Vite 保留主 JS chunk 超過 500 kB 的警告。

## 未執行

- 沒有 fresh native target scan，也沒有接觸 `example.com` 或 `127.0.0.1:9001`。
- 沒有 clean install、UAC、update、uninstall 或 installed Start → Progress → report → reopen → export。
- Browser preview 證明設定、持久化與介面流程，不代表真正安全掃描已完成。
